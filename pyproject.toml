[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = [
    "E",  # pycodestyle
    "W",  # pycodestyle
    "F",  # pyflakes
    "UP", # pyupgrade
    "B",  # flake8-bugbear
    "SIM", # flake8-simplify
    "I",  # isort
    "ARG", # flake8-unused-arguments
    "C4", # flake8-comprehensions
    "RUF", # Ruff-specific rules
]

ignore = [
    "E501", # line too long (handled by formatter)
    "E731", # lambda assignment
    "E402", # module level import not at top (common in HA)
    "ARG002", # unused method argument
    "ARG001", # unused function argument
    "SIM102", # collapsible if
    "SIM108", # ternary operator
    "UP006", # non-pep604-annotation (for Python 3.9 compat)
    "UP007", # non-pep604-annotation
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG"]

[tool.ruff.lint.isort]
combine-as-imports = true
force-sort-within-sections = true
known-first-party = ["custom_components.retention_cleaner"]

[tool.ruff.lint.mccabe]
max-complexity = 15

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--disable-warnings",
    "-ra",
    "--cov=custom_components.retention_cleaner",
    "--cov-fail-under=90",
]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["custom_components.retention_cleaner"]
omit = ["tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
